begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "big_comment"
    gem.summary = "Rails engine for handling comment threads."
    gem.email = "tony+bigcomment@tonystubblebine.com"
    gem.authors = ["Tony Stubblebine"]
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
  end
rescue
  puts "Jeweler or dependency not available."
end
